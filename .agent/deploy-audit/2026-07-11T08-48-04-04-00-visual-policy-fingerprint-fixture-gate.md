# Deploy Audit: Visual Policy Fingerprint Fixture Gate

**Timestamp:** `2026-07-11T08-48-04-04-00`

## Summary

The repository has no root package test gate. The current runtime change can reach Pages without executable proof of the exact module graph, joined creature topology, grass policy, shadow policy or committed-frame correlation.

## Plan ledger

**Goal:** prevent deployment from treating source inspection or a renderer label as proof of visible output.

- [x] Inventory the current executable product gate.
- [x] Identify missing CPU and browser fixtures.
- [x] Define the ordered gate.
- [x] Keep deployment configuration unchanged in this documentation pass.

## Current gate

```txt
root package.json: absent
root npm test: absent
product visual-policy fixture: absent
browser smoke: absent
Pages pixel/screenshot smoke: absent
```

## Required CPU fixtures

```txt
node scripts/prehistoric-rush-patch-activation-fixture.mjs
node scripts/procedural-creature-geometry-identity-fixture.mjs
node scripts/prehistoric-rush-module-graph-fixture.mjs
node scripts/prehistoric-rush-visual-policy-fixture.mjs
node scripts/prehistoric-rush-render-binding-fixture.mjs
node scripts/prehistoric-rush-frame-receipt-fixture.mjs
node scripts/prehistoric-rush-lifecycle-fixture.mjs
```

## Required browser smoke

```txt
joined neck/head has no open seam
FrontSide creature exterior remains visible
grass cards have intended alpha silhouette
grass cards do not render as black rectangles
tree crowns do not cast large polygon shadows
trunks/player/terrain follow declared caster policy
shadow frustum and bias avoid clipping/acne
frame readback names accepted policy and bindings
retry rejects old run/stream/resource callbacks
```

## Required deployed proof

```txt
Pages loads the exact module graph
host observation reports expected fingerprints
first committed frame receipt exists
visual screenshot/pixel baseline matches accepted policy
no console module-load or binding failures
```

## Ordered gate

```txt
patch activation fixture
  -> module graph fixture
  -> creature exact geometry fixture
  -> grass/shadow visual-policy fixture
  -> binding fixture
  -> frame receipt fixture
  -> lifecycle fixture
  -> local browser smoke
  -> deployed Pages smoke
```

## Deployment blockers

```txt
controller/consumer patch parity absent
module graph fingerprint absent
exact creature geometry identity absent
grass and shadow descriptors absent
binding results absent
frame receipt absent
product fixture command absent
browser/Pages visual smoke absent
```

## Current status

```txt
runtime source changed by this pass: no
package scripts changed: no
deployment workflow changed: no
new fixtures implemented: no
new fixtures run: no
browser smoke run: no
Pages smoke run: no
```