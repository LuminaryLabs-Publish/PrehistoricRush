# Turn Ledger: 2026-07-11T02-41-37-04-00

## Summary

Selected `LuminaryLabs-Publish/PrehistoricRush` after comparing the ten accessible Publish repositories with the central ledger. The repo had a newly landed seeded patch controller, deterministic patch generator, Worker adapter and stable tree batch integration that were newer than its existing `.agent` audit state.

## Plan ledger

**Goal:** Document the streamed-world implementation and identify the next safe proof boundary without changing runtime behavior.

- [x] Excluded `TheCavalryOfRome`.
- [x] Worked on one repository only.
- [x] Read the current root `.agent` routing state.
- [x] Read `src/game.js`.
- [x] Read `src/world/prehistoric-patch-generator.js`.
- [x] Read `src/workers/prehistoric-patch-worker.js`.
- [x] Read `prehistoric-rush-domain-kit.js`.
- [x] Read the pinned `seeded-world-patch-controller-kit` implementation and service boundary.
- [x] Identified the interaction loop, domains, services and kits.
- [x] Added timestamped architecture, render, gameplay, interaction, world-streaming and deploy audits.
- [x] Refreshed root `.agent` files and registry.
- [ ] Runtime implementation remains future work.
- [ ] Fixtures and browser validation remain future work.

## Runtime changes observed since the previous audit

```txt
instanced-render-batch-kit added and consumed by tree pools
seeded-world-patch-controller-kit added and consumed by the parent domain
active window reduced from radius 3 to radius 2
retain and prefetch rings added
cache limit and per-frame generation/activation budgets added
deterministic patch content extracted from the Three host
optional module Worker generation added
terrain changed to 25 reusable slots
tree content changed to per-cell batch replacement/release
grass, shards and colliders changed to active-patch rebuilds
host snapshot gained patch streaming state
```

## Finding

Patch generation is now separated from rendering, which removes the largest synchronous generation pass from the frame boundary. The remaining blocker is activation authority: the controller marks a patch active before terrain, tree, grass, shard, collider, physics and height-sampler consumers complete one validated atomic commit.

## Next safe ledge

```txt
PrehistoricRush Seeded Patch Activation Authority
+ Worker / Render / Physics Parity Fixture Gate
```

## Write policy

```txt
repository: LuminaryLabs-Publish/PrehistoricRush
branch: main
runtime source changed: no
new branch: no
pull request: no
```
