# Turn Ledger: PrehistoricRush Character Creator Authority

**Timestamp:** `2026-07-11T21-00-00-04-00`

## Summary

This turn synchronized `PrehistoricRush` documentation with the current creator implementation. It records that route hosts, game profile admission and the shared creature adapter now exist, then isolates the remaining draft-commit, preview-state and projection-fit authority gaps.

## Plan ledger

**Goal:** leave one complete source-backed audit state and one ordered implementation ledge.

- [x] Inventory Publish repositories.
- [x] Compare central ledgers and root `.agent` state.
- [x] Select only `PrehistoricRush`.
- [x] Exclude `TheCavalryOfRome`.
- [x] Read current runtime and profile sources.
- [x] Refresh required root `.agent` documents.
- [x] Add timestamped architecture, render, gameplay, interaction, creator and deploy audits.
- [x] Push directly to `main` without a branch or PR.
- [ ] Implement runtime changes.
- [ ] Run executable fixtures.

## Source changes observed before this audit

```txt
character creator UI condensed and centered
visible settings reduced to Size, Body, Tail, Legs, Skin and Belly
profile JSON removed from the page
real procedural creature showcase retained
shared Three creature adapter retained
camera framing now updates from geometry bounds every frame
workflow trigger files added and removed around framing correction
```

## Main findings

```txt
partial-patch debounce can discard an earlier unsaved edit group
stored profile revision is reused for unsaved preview descriptors
Ready can be projected before morph convergence
Saved lacks visible-frame acknowledgement
framing ignores horizontal FOV, aspect and posed skinned bounds
```

## Current next ledge

```txt
PrehistoricRush Character Creator Draft + Commit + Preview Frame Authority
```

## Validation

```txt
documentation-only: yes
runtime source changed: no
branch created: no
pull request created: no
target branch: main
browser smoke: not run
Pages smoke: not run
creator fixtures: not implemented
```