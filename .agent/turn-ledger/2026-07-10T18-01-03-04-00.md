# Turn Ledger: PrehistoricRush

Timestamp: `2026-07-10T18-01-03-04-00`

## Goal

Document the current `PrehistoricRush` interaction loop, domains, kit services, and next safe implementation boundary after applying the oldest eligible documented-selection rule.

## Checklist

- [x] Listed the complete accessible `LuminaryLabs-Publish` installation.
- [x] Excluded `TheCavalryOfRome`.
- [x] Compared all nine eligible repositories against central ledger recency.
- [x] Confirmed root `.agent` state for the selected repository.
- [x] Selected only `LuminaryLabs-Publish/PrehistoricRush`.
- [x] Read current source, runtime/domain owners, manifests, prior audits, and latest commits.
- [x] Identified the interaction loop.
- [x] Identified domains in use.
- [x] Identified implemented, external, and runtime-implied kits.
- [x] Identified services offered by those kits.
- [x] Added architecture, render, gameplay, interaction, external-kit, lifecycle, and deploy audits.
- [x] Refreshed root `.agent` documents and kit registry.
- [x] Changed no runtime source.
- [x] Created no branch or pull request.
- [x] Targeted `main` only.

## Finding

The route has four coupled proof gaps:

```txt
runtime source admission
single frame/render ownership
real session start/restart transactions
mount/dispose/remount resource ownership
```

Three.js and Rapier use semver-pinned CDN paths, but the physics kit is resolved from mutable `NexusRealtime-ProtoKits@main`. The loader suppresses import failures, runtime fallback is not classified, and host readback cannot prove which module source or capability set was admitted. The route also retains two RAF loops and global browser/GPU/physics resources without disposal authority.

## Decision

Do not begin visual or terrain work. Update existing runtime/domain owners first and add only the shared admission/result/lifecycle capabilities required to prove deterministic boot, frame, restart, dispose, and remount behavior.

## Next ledge

```txt
PrehistoricRush Runtime Dependency Admission + Single-Owner Session Lifecycle Fixture Gate
```

## Validation state

```txt
runtime changed: no
root package.json: absent
npm checks: unavailable
browser smoke: not run
runtime admission fixture: unavailable
single-frame fixture: unavailable
restart fixture: unavailable
dispose/remount fixture: unavailable
branch created: no
pull request created: no
```