# Turn Ledger: PrehistoricRush Run Start and Restart Admission

**Timestamp:** `2026-07-12T21-51-38-04-00`  
**Repository:** `LuminaryLabs-Publish/PrehistoricRush`  
**Branch:** `main`  
**Scope:** documentation only

## Summary

Selected PrehistoricRush through the oldest eligible current-audit fallback. Audited UI, Space, Enter, direct start, key repeat, run generation, simulation reset, streaming/Worker reuse, physics, camera, rendering, and first-frame proof.

## Plan ledger

**Goal:** leave a complete timestamped start/restart breakdown without changing runtime behavior.

- [x] Compare the full Publish inventory and central ledgers.
- [x] Exclude `TheCavalryOfRome`.
- [x] Select only `PrehistoricRush`.
- [x] Inspect `src/game.js` and the product domain kit.
- [x] Record interaction loop, domains, 45 surfaces, and services.
- [x] Define run start/restart authority and fixtures.
- [x] Add required root and system audit documents.
- [x] Update central tracking on `main`.
- [x] Create no branch or pull request.
- [ ] Implement runtime changes later.

## Findings

```txt
Enter invokes start regardless of active status
KeyboardEvent.repeat is not rejected
domain start has no scene/status/command admission
start resets RunState InputState and simulation resolution only
host-local input survives and is reapplied next frame
patch controller Worker queue/cache and active content lack run generation
physics camera render and HUD have no atomic participant receipt
no typed exactly-once start result or first visible run-generation frame exists
```

## Resulting authority

```txt
prehistoric-rush-run-start-restart-admission-authority-domain
```

## Validation boundary

No runtime, input, simulation, physics, streaming, Worker, camera, render, package, workflow, or deployment source changed. No fixture was executed.