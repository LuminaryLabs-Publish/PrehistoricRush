# Turn Ledger: PrehistoricRush Viewport Surface Audit

**Timestamp:** `2026-07-13T00-49-53-04-00`  
**Repository:** `LuminaryLabs-Publish/PrehistoricRush`  
**Branch:** `main`

## Summary

Selected PrehistoricRush through the oldest eligible documented-repository fallback after confirming the complete Publish inventory, Cavalry exclusion, central-ledger coverage and root `.agent` coverage. This run documents the missing authority between host measurement, DPR, camera projection, WebGL drawing-buffer allocation and the first visible resized frame.

## Plan ledger

**Goal:** preserve one source-backed record of what was selected, inspected, added and not implemented.

- [x] Compare ten accessible Publish repositories.
- [x] Exclude `TheCavalryOfRome`.
- [x] Select only `PrehistoricRush`.
- [x] Inspect `game.html`, `src/pages/game.js`, `src/game.js`, product domain, resolution policy, patch generator, package scripts and prior `.agent` state.
- [x] Record interaction loop, domains, 45 surfaces and offered services.
- [x] Add architecture, render, gameplay, interaction, viewport and deployment audits.
- [x] Refresh root `.agent` routing and registry.
- [x] Push directly to `main` with no branch or pull request.
- [ ] Implement and execute viewport fixtures later.

## Main finding

The game uses `innerWidth`, `innerHeight` and a startup-only DPR sample. Resize directly mutates camera and renderer state without measuring the host, refreshing DPR, admitting one revisioned candidate, enforcing a pixel budget or acknowledging the first visible frame produced from the new surface.

## Change boundary

Documentation only. No runtime, renderer, simulation, package, dependency or deployment behavior changed.