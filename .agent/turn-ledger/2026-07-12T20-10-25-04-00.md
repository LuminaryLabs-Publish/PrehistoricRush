# Turn Ledger: PrehistoricRush Runtime Lifecycle Audit

**Timestamp:** `2026-07-12T20-10-25-04-00`  
**Repository:** `LuminaryLabs-Publish/PrehistoricRush`  
**Branch:** `main`

## Summary

Selected PrehistoricRush through the oldest-eligible rule after comparing all ten Publish repositories and excluding TheCavalryOfRome. Reviewed the current game boot, engine/physics composition, Worker streaming, Three.js resource construction, browser listeners, recursive RAF and public host. Added a documentation-only authority map for deterministic stop, resource retirement and re-entry proof.

## Plan ledger

**Goal:** document one lifecycle owner for every callback, asynchronous executor, public capability and render resource created by the game page.

- [x] Compare organization inventory with central ledger.
- [x] Confirm all eligible repositories are tracked and root-documented.
- [x] Select only PrehistoricRush.
- [x] Review `game.html`, `src/pages/game.js`, `src/game.js`, `src/render/three-procedural-creature.js` and creator shutdown behavior.
- [x] Preserve the 45-surface kit and service inventory.
- [x] Record source-level resource counts without claiming measured memory retention.
- [x] Define start, running, stopping, stopped and failed lifecycle states.
- [x] Define callback, Worker, engine, physics, renderer and public-host retirement order.
- [x] Add architecture, render, gameplay, interaction, runtime-lifecycle and deploy audits.
- [x] Refresh root `.agent` routing and validation records.
- [x] Synchronize central tracking on `main`.
- [ ] Implement and execute lifecycle fixtures.

## Finding

The game page owns no explicit stop surface. It does not retain the RAF ID, listener functions, Worker/executor retirement handle, controller/provider retirement result or renderer-resource lease. `createThreeAdapter()` has no `dispose()` return, and the existing `disposeCreatureMesh()` helper is not imported by the game runtime.

## Change scope

```txt
runtime source changed: no
gameplay changed: no
rendering changed: no
package/dependencies changed: no
deployment changed: no
branch created: no
pull request created: no
.agent documentation changed: yes
central ledger changed: yes
```
