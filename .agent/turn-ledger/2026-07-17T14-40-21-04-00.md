# PrehistoricRush Turn Ledger

**Timestamp:** `2026-07-17T14-40-21-04-00`  
**Selected repository:** `LuminaryLabs-Publish/PrehistoricRush`  
**Selection reason:** sole runtime-ahead eligible repository  
**Reviewed pre-audit head:** `47788818edec7d49753f942a69ef392a8b092037`

## Summary

Compared all 11 `LuminaryLabs-Publish` repositories against the ten eligible central ledgers and root `.agent` states. `TheCavalryOfRome` was excluded. PrehistoricRush was one commit ahead of its documented head because `src/shared/runtime-versions.js` moved the pinned Nexus Engine revision to `d41992636de2752f1ad9047b80701e6313f19b87`.

The corresponding Nexus Engine commit fixes the Foliage service binding from an undefined `createPlacementRecipe` identifier to `createFoliagePlacementRecipe`. The product uses that provider in both the browser main realm and the patch Worker.

## Work completed

- Added a timestamped project breakdown.
- Added architecture, render, gameplay, interaction, vegetation-system, deploy and central-sync audits.
- Refreshed root `.agent` entry points and kit registry.
- Preserved the complete 97-surface kit/service inventory.
- Defined a 20-surface provider-admission and realm-parity authority.
- Changed no runtime, test, package, workflow or deployment behavior.

## Finding

The pin repair is source-backed, but current product tests do not import the pinned Nexus provider or execute the production Vegetation runtime. Main-thread and Worker startup also do not publish an exact provider-revision parity result before streaming begins.

## Validation

Source inspection only. `npm test`, live CDN import, browser construction, Worker parity, artifact and Pages fixtures were not run. No branch or pull request was created.