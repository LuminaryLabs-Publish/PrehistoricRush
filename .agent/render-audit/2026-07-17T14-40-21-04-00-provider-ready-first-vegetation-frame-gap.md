# Provider-Ready First Vegetation Frame Gap

**Timestamp:** `2026-07-17T14-40-21-04-00`

## Summary

The rendered forest depends on a provider imported from the pinned Nexus Engine revision. The main realm constructs Vegetation and feeds patch data into Three.js layers; the Worker can construct the same runtime and generate patches. Neither render evidence nor startup receipts bind the visible vegetation frame to an exact accepted provider revision and cross-realm parity result.

## Current render path

```txt
pinned provider import
  -> product Vegetation catalog
  -> main or Worker patch generation
  -> patch-controller activation
  -> tree fidelity, foliage-card and ground-cover layers
  -> WebGL frame
```

## Missing evidence

```txt
accepted provider revision on frame: absent
main provider probe digest: absent
Worker provider probe digest: absent
catalog parity digest: absent
patch provider generation identity: absent
render-frame provider generation identity: absent
FirstProviderBoundVegetationFrameAck: absent
```

A successful local module import does not prove that the CDN provider can construct the Foliage domain. A worker-ready count also does not prove that the visible frame uses a patch generated by the same admitted provider generation.

## Required render settlement

```txt
ProviderRealmParityResult
  -> attach ProviderGeneration to patch requests/results
  -> reject stale provider generations during activation
  -> attach ProviderGeneration to render digest
  -> publish FirstProviderBoundVegetationFrameAck
```

## Validation boundary

No blank forest, missing foliage or deployed rendering incident was reproduced. Browser, Worker, artifact and Pages frame fixtures were not run.